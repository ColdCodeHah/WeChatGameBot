# -*- mode: python -*-

block_cipher = None


a = Analysis(['Files\\Python3.6\\Lib\\site-packages', 'F:\\Python\\Test\\WeChatBot\\Bot.py'],
             pathex=['E:\\Program', 'F:\\Python\\Test\\WeChatBot'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          name='site-packages',
          debug=False,
          strip=False,
          upx=True,
          runtime_tmpdir=None,
          console=True )
